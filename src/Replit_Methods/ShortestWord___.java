package Replit_Methods;

import java.util.Arrays;

public class ShortestWord___ {
    public static void main(String[] args) {
       String[] words={"olive", "fish", "pursuit", "old", "warning", "python", "java", "coffee", "cat", "ray"};
  String shortestWord=words[0];
        for (String s : words) {

            //String[] wr=s.split(", ");
            if (s.length()<shortestWord.length()){

            //shortestWord+=s;

            } System.out.println(shortestWord);

           // System.out.println(Arrays.toString(wr));
           // if (str.length<wr.length()){

            }

        System.out.println(shortestWord);
            }

        }





